export default {
    isReversal(state) {
        return state.isReversal;
    },
    isAutoSave(state) {
        return state.isAutoSave;
    },
    isKeepAuth(state) {
        return state.isKeepAuth;
    }
};