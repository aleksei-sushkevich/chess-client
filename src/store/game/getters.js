export default {
    round(state) {
        return state.round;
    },
    history(state) {
        return state.history;
    },
    gamesHistory(state) {
        return state.gamesHistory;
    },
    winner(state) {
        return state.winner;
    }
}